# Test Details Configuration
#
# This file defines the repositories and components that need to be cloned and built
# for conducting each automated test in the GitLab CI pipeline.

tests:

 #------------------------------------ SOFTWARE TESTS ------------------------------------
  software:

    # ----- UNIT TESTS -----
    unit:

      SiFive_decoder_library_test:
        repositories:
          - name: sifive-trace-decoder
            components:
              - dqr_decoder_dll

    # ----- INTEGRATION TESTS -----
    integration:

 #------------------------------------ HARDWARE TESTS ------------------------------------
  hardware:

    # ----- UNIT TESTS -----
    unit:

    # ----- INTEGRATION TESTS -----
    integration: